[build]
  publish = "public"

# The /vless-xhttp route is handled by the Edge Function in netlify/edge-functions/xhttp.ts
# This allows for dynamic backend configuration via the VLESS_BACKEND environment variable.

# Fallback or other redirects can go here
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200